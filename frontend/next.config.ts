import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["i.imgur.com", "source.unsplash.com", "images.unsplash.com"],
  },
  // other config options here
};

export default nextConfig;
